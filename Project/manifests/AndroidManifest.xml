<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.catataja">

    <!-- Permission untuk akses internet dan lokasi -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Catataja"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Halaman pertama: Splash Logo -->
        <activity android:name=".SplashLogoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Halaman kedua: Welcome + bendera + greeting -->
        <activity android:name=".WelcomeActivity" />

        <!-- Dashboard berisi list catatan -->
        <activity android:name=".DashboardActivity" />

        <!-- Tambah catatan -->
        <activity android:name=".AddNoteActivity" />

        <!-- Detail catatan -->
        <activity android:name=".DetailNoteActivity" />

        <!-- Edit catatan -->
        <activity android:name=".EditNoteActivity" />

    </application>

</manifest>